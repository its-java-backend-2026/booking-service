# Copiare in .env e cambiare i valori.
#   cp .env.example .env
#
# Il .env NON va nel repository: e' nel .gitignore. Nel repository ci va
# QUESTO file, che dice quali variabili servono senza rivelarne i valori.
BOOKING_DB_NAME=booking_db
BOOKING_DB_USER=cinema
BOOKING_DB_PASSWORD=cinema

# Porta sull'HOST del database. 5433 e non 5432: sul portatile la 5432 e'
# gia' di shows-db (e del PostgreSQL installato al G2).
BOOKING_DB_PORT=5433

# Porta sull'HOST del servizio.
BOOKING_PORT=8083

# I servizi a valle NON stanno in questo compose (sono negli altri due
# repository). Il default punta all'host: utile se girano dall'IDE.
# Per il sistema completo si usa cinema-deploy.
CINEMA_SHOWS_URL=http://host.docker.internal:8081
CINEMA_PRICING_URL=http://host.docker.internal:8082
